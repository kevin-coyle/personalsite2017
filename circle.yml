machine:
  php:
    version: 7.1.3
dependencies:
  post:
    - cp .env.aws .env
    - php artisan key:generate
  pre:
    - sudo pip install 'awsebcli==3.7.4' --force-reinstall
deployment:
  production:
    branch: master
    commands:
      - bash ./setup-eb.sh
      - eb deploy
