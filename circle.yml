machine:
  php:
    version: 7.1.3
dependencies:
  post:
    - cp .env.heroku .env
    - php artisan key:generate
deployment:
  staging:
    branch: master
    heroku:
      appname: infinite-temple-60838